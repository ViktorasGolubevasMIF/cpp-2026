(from a dictionary definition: opaque: adjective; not able to be seen through; not transparent).

/*
Synonym terms for the opaque pointer idiom in C/C++ include 'pointer to implementation' (pImpl), 'impl pointer', and 'handle'. 
These terms refer to the technique of hiding the internal structure of a class or data type behind a forward-declared pointer, promoting encapsulation and reducing compilation dependencies.
*/

/*
The term “forward declaration” is used in multiple programming languages and refers to a declaration that announces an entity to be more completely defined elsewhere. In some languages, forward declarations have their own syntax (e.g., Pascal has a forward keyword for that purpose) but in C and C++, it’s just an ordinary declaration that is not a definition.

Whereas function prototypes are always for function entities, forward declarations can also be for other entities and classes in particular:
*/

https://andreasfertig.com/blog/2025/10/forward-declaring-a-type-in-cpp-the-good-and-the-bad/
